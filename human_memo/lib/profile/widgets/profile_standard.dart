import 'package:flutter/material.dart';

class ProfileStandard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("麒麟さん", style: Theme.of(context).textTheme.headline3),
      ],
    );
  }
}
